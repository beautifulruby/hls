# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in hls.gemspec
gemspec

gem "irb"
gem "rake", "~> 13.0"

gem "rspec", "~> 3.0"

# For Railtie integration testing. The gem itself does not require
# Rails — the Railtie is only loaded when Rails is defined.
gem "railties", "~> 7.0", group: :development
gem "activesupport", "~> 7.0", group: :development
gem "activejob", "~> 7.0", group: :development
